//using imagerecognition;
//using system;
//using system.collections.generic;
//using system.configuration;
//using system.io;
//using system.linq;
//using system.text;
//using system.threading.tasks;

namespace imagerecognizerconsole
{
    class program
    {
        //private static string m_trainingimagesfoldername;
        public void main(string[] args)
        {
            //train();

            //training.save();

            //training.dotraining(trainingbinaryfilepath, totaldatacount);

            //string testingbinaryfilename = "positivetestbinaryfile.txt";
            //testingbinaryfilepath = path.combine(tempfolder, testingbinaryfilename);
            //string testingdata = path.combine(startuppath, "testres\\positivetestdata");
            //totaldatacount = imageutil.generatebinaryfile(testingbinaryfilepath, testingdata);

            //assert.isnotnull(training.getnetwork());

            //deeplearningtesting testing = new deeplearningtesting();
            //int totalmatchedcount = testing.dotesting(training.getnetwork(), testingbinaryfilepath, totaldatacount);
        }

        //private static void trainnewnetwork()
        //{
        //    m_trainingimagesfoldername = configurationmanager.appsettings["trainingimagesfoldername"];

        //    var files = directory.getfiles(m_trainingimagesfoldername);

        //    deeplearningtraining training = new deeplearningtraining(/*"tempfolder"*/);

        //    foreach (var file in files)
        //    {
        //        //var convertedfile = training.convertimage(file);
        //        training.dotraining(file);
        //    }

        //    //save(trainednetworkfilename);
        //}

        //private static void trainexistingnetwork()
        //{
        //    m_trainingimagesfoldername = configurationmanager.appsettings["trainingimagesfoldername"];

        //    var files = directory.getfiles(m_trainingimagesfoldername);

        //    //deeplearningtraining training = new deeplearningtraining.load(trainednetworkfilename));

        //    foreach (var file in files)
        //    {
        //        //var convertedfile = training.convertimage(file);
        //        //training.dotraining(file);
        //    }

        //    //save(trainednetworkfilename);
        //}

        //private static void test()
        //{
        //    m_trainingimagesfoldername = configurationmanager.appsettings["testimagesfoldername"];

        //    var files = directory.getfiles(m_trainingimagesfoldername);

        //    //deeplearningtraining training = new deeplearningtraining.load(trainednetworkfilename));

        //    foreach (var file in files)
        //    {
        //        //var convertedfile = training.convertimage(file);
        //        // double percent = training.testimage(file);
        //    }
        //}
    }
}
